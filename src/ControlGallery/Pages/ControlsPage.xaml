<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/maui/global"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"x:Class="ControlGallery.Pages.ControlsPage" 
             Title="Controls">
    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="{FontImageSource Glyph={x:Static a:IconFont.Boxes}, FontFamily=FontAwesome}" Clicked="ToolbarItem_Clicked"/>
        <ToolbarItem IconImageSource="{FontImageSource Glyph={x:Static a:IconFont.List}, FontFamily=FontAwesome}" Clicked="ToolbarItem_Clicked_1"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Resources>
        <DataTemplate x:Key="ListTemplate">
            <Grid ColumnDefinitions="44,*" RowDefinitions="44,1">
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NavigateTo}"/>
                </Grid.GestureRecognizers>
                <Image Source="{Binding Icon}"
                    VerticalOptions="Center"
                    HorizontalOptions="Center"/>
                <Label Grid.Column="1" 
                    VerticalOptions="Center"
                    Text="{Binding Title}"
                />
                <BoxView Color="{StaticResource Gray200}" Grid.ColumnSpan="2" Grid.Row="1"/>
                <Grid.Behaviors>
                    <CursorBehavior/>
                </Grid.Behaviors>
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="GridTemplate">
            <Border Stroke="{StaticResource Gray200Brush}" StrokeThickness="1" CursorBehavior.AttachBehavior="True">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="8"/>
                </Border.StrokeShape>
                <Grid ColumnDefinitions="*"
                      RowDefinitions="13*,8*"
                      MinimumHeightRequest="100"
                      >
                    <Grid.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding NavigateTo}"/>
                    </Grid.GestureRecognizers>
                    <Image Source="{Binding Icon}"
                            Grid.Row="0"
                        VerticalOptions="Center"
                        HorizontalOptions="Center"/>
                    <Label Grid.Row="1"
                        VerticalOptions="Center"
                        HorizontalOptions="Center"
                        Text="{Binding Title}"
                    />
                    <Grid.Behaviors>
                        <CursorBehavior/>
                    </Grid.Behaviors>
                </Grid>
            </Border>
        </DataTemplate>

        <DisplayTemplateSelector x:Key="TemplateSelector"
                                   GridTemplate="{StaticResource GridTemplate}"
                                   ListTemplate="{StaticResource ListTemplate}"/>

    </ContentPage.Resources>
    
    <CollectionView Grid.Row="1" x:Name="ControlsList"
                    ContentInsetAdjustmentBehavior.ContentInset="{StaticResource PageInset}"
                    ItemTemplate="{StaticResource TemplateSelector}">
        <CollectionView.Effects>
            <ContentInsetAdjustmentBehaviorRoutingEffect/>
        </CollectionView.Effects>
        <CollectionView.ItemsSource>
            <x:Array Type="{x:Type m:NavItem}">
                <NavItem Title="ActivityIndicator"
                            Icon="view_activityindicator.png"
                            Destination="{x:Type p:ActivityIndicatorPage}"/>
                <NavItem Title="Alerts"
                            Destination="{x:Type p:AlertPage}"/>
                <NavItem Title="Borders"
                            Destination="{x:Type p:BorderPage}"/>
                <NavItem Title="BoxView"
                            Destination="{x:Type p:BoxViewPage}"/>
                <NavItem Title="Buttons"
                            Icon="view_button.png"
                            Destination="{x:Type p:ButtonPage}"/>
                <NavItem Title="CheckBoxes"
                            Destination="{x:Type p:CheckboxPage}"/>
                <NavItem Title="CarouselView"
                            Icon="view_listview.png"
                            Destination="{x:Type p:CarouselViewPage}"/>
                <NavItem Title="CollectionView"
                            Icon="view_listview.png"
                            Destination="{x:Type c:CollectionViewPage}"/>
                <NavItem Title="Date Picker"
                            Icon="view_datepicker.png"
                            Destination="{x:Type p:DatePickerPage}"/>
                <NavItem Title="Editors"
                            Icon="view_editor.png"
                            Destination="{x:Type p:EditorPage}"/>
                <NavItem Title="Entry"
                            Icon="view_entry.png"
                            Destination="{x:Type p:EntryPage}"/>
                <NavItem Title="Frame"
                            Destination="{x:Type p:FramePage}"/>
                <NavItem Title="Images"
                            Icon="view_image.png"
                            Destination="{x:Type p:ImagePage}"/>
                <NavItem Title="Labels"
                            Icon="view_label.png"
                            Destination="{x:Type p:LabelPage}"/>
                <NavItem Title="Map"
                            Icon="view_map.png"
                            Destination="{x:Type p:MapPage}"/>
                    <NavItem Title="Picker"
                            Icon="view_picker.png"
                            Destination="{x:Type p:PickerPage}"/>
                    <NavItem Title="Progress Bars"
                            Icon="view_progressbar.png"
                            Destination="{x:Type p:ProgressBarPage}"/>
                <NavItem Title="Radio Buttons"
                            Destination="{x:Type p:RadioButtonPage}"/>
                <NavItem Title="RefreshView"
                            Destination="{x:Type p:RefreshViewPage}"/>
                <NavItem Title="SearchBar"
                            Icon="view_searchbar.png"
                            Destination="{x:Type p:SearchBarPage}"/>
                <NavItem Title="Shapes"
                            Destination="{x:Type p:ShapesPage}"/>
                <NavItem Title="Sliders"
                            Icon="view_slider.png"
                            Destination="{x:Type p:SliderPage}"/>
                <NavItem Title="Steppers"
                            Icon="view_stepper.png"
                            Destination="{x:Type p:StepperPage}"/>
                <NavItem Title="Switches"
                            Icon="view_switch.png"
                            Destination="{x:Type p:SwitchPage}"/>
                <NavItem Title="TableView"
                            Icon="view_tableview.png"
                            Destination="{x:Type p:TableViewPage}"/>
                    <NavItem Title="TimePicker"
                            Icon="view_timepicker.png"
                            Destination="{x:Type p:TimePickerPage}"/>
                    <NavItem Title="WebView"
                            Icon="view_webview.png"
                            Destination="{x:Type p:WebViewPage}"/>
                <NavItem Title="VirtualListView"
                           Icon="view_listview.png"
                           Destination="{x:Type p:VirtualListViewPage}"/>
                </x:Array>
        </CollectionView.ItemsSource>
    </CollectionView>        
</ContentPage>