<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
             x:Class="Animations.ScalePage"
             Title="Scale">
			 
    <ScrollView>
        <VerticalStackLayout 
            Spacing="25" 
            Padding="30,0" 
            VerticalOptions="Center">

            <Image
                x:Name="BotImg"
                Source="dotnet_bot.png"
                SemanticProperties.Description="Cute dot net bot waving hi to you!"
                HeightRequest="200"
                HorizontalOptions="Center" />
                
            <Button 
                Text="Say Bye Bye"
                SemanticProperties.Hint="Shrinks the dot net bot so it seems to disappear"
                Clicked="OnClickedAsync"
                HorizontalOptions="Center" />

        </VerticalStackLayout>
    </ScrollView>
 
</ContentPage>
